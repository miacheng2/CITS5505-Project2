
var number0fStudentrejected = 0;

function sayhi() {
    alert("HI2")
}

function factorial(n) {
    let rec = factorial(n - 1)
    return n * rec
}

alter(5);
factorial(5);


// 假设这个变量在全局作用域中声明和初始化了
var numberOfStudentsRejected = 0;  // 使用 camelCase 命名方式，并正确地使用大写字母 O

function ejectStudent() {
    numberOfStudentsRejected += 1;  // 修正变量名
}

function displayNumberOfStudentsEjected() {
    alert("number of students" + numberOfStudentsRejected);  // 修正函数名和拼写错误
}

function displayNumberOfStudentsEjected() {
    prompt("Give me a number");  // 修正函数名和拼写错误
}
